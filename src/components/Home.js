import React from 'react';
import Button from './Button'; 

const Home = ()=> {
  return(
    <main>
      <h1>Home Page</h1>
      <Button>
        Buy Now
      </Button>
    </main>
  );
}

export default Home;